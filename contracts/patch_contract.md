# Patch Contract

A patch is a proposed change to the persistent scientific world state.

## Requirements
- Must be backed by a valid execution receipt.
- Must follow the schema specified in the registry.
- Must be validated by a THETA gate.

